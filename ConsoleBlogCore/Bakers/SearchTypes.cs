namespace ConsoleBlogCore.Bakers {
    enum SearchTypes {
        SearchByBlogName,
        SearchByBlogId,
        SearchByPostTitle,
        SearchByPostContent,
        SearchByPostId, 
        SearchError // For exits or Errors
    }
}